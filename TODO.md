# 开发进度跟踪 (TODO)

本文档用于记录和跟踪 AI Drama Studio SaaS 商业化重构的开发进度。

## 阶段 1：账号与商业化鉴权体系改造 (✅ 已完成)
- [x] 更新 `src/lib/auth.ts`，增加手机号验证码登录（当前为存根）与微信登录功能。
- [x] 改造 `signin` 和 `signup` 页面 UI，适配国内用户习惯（账号密码、手机号切换 Tab，底部微信快捷入口）。
- [x] 审计后端 API 路由（如 `src/lib/assets/services/asset-actions.ts`），严格通过 `projectId` 校验阻止跨租户越权操作。
- [x] 验证 NextAuth 会话回调正确返回 `id` 字段以供安全中间件使用。

## 阶段 1.5：SaaS 商业化前端体验升级 (✅ 已完成)
- [x] 重构 Workspace 工作区，实现专业级的三栏可拖拽自适应布局（左侧导航/资产库，右侧属性配置）。
- [x] 个人中心总览：集成账号核心数据展示、每日签到组件、邀请奖励面板（邀请码与专属链接）。
- [x] 个人中心令牌：实现类 SaaS 标准的 API 密钥管理面板（状态开关、脱敏展示、复制/查看快捷键及消费统计）。
- [x] 个人中心日志：实现消费与任务维度的明细日志列表，搭配多条件筛选栏与宏观指标（RPM/TPM/总金额）。
- [x] 个人中心充值：构建现代化的套餐 Pricing 页面结构与 UI。

## 阶段 2：积分与虚拟货币系统 (⏳ 待开发)
- [ ] 扩展 Prisma Schema，添加 `PointAccount`（用户积分账户）与 `PointTransaction`（积分流水表）。
- [ ] 开发积分变动服务层逻辑（增加、扣减积分操作，附带事务保证）。
- [ ] 开发前端积分展示组件（导航栏顶部展示用户当前余额）。
- [ ] 实现每日签到或新用户注册免费赠送额度的基础逻辑。

## 阶段 3：模型提供商对接与计费扣减逻辑 (⏳ 待开发)
- [ ] 根据请求的模型类型（如图像生成、视频生成、LLM 对话）定义每次操作的积分消耗标准。
- [ ] 拦截耗时/耗费资源的核心操作（如触发生成任务前），先校验用户余额是否充足。
- [ ] 在任务回调或同步执行完成后，进行实际的积分扣减并写入 `PointTransaction`。

## 阶段 4：支付网关接入与充值模块 (⏳ 待开发)
- [ ] 创建充值套餐（如：￥9.9 购买 1000 积分，￥29.9 购买 3500 积分等）。
- [ ] 开发支付回调接口（Webhook），监听支付成功状态并为用户发放对应积分。
- [ ] 接入国内常见支付网关（微信支付 / 支付宝）。
- [ ] 开发前端充值页面与支付二维码弹窗组件。

---
*注：每次完成新功能后，请及时更新此文档的勾选状态，以便随时把握整体进度。*
